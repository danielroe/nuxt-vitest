{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "test:nuxt": "vitest -c vitest.config.nuxt.ts",
    "test:unit": "vitest -c vitest.config.unit.ts",
    "test": "pnpm test:unit --run && pnpm test:nuxt --run"
  },
  "devDependencies": {
    "nuxt": "3.0.0",
    "vitest": "0.26.3",
    "vitest-environment-nuxt": "latest"
  }
}
